export default function SettingsPage() {
  return (
    <main className="page">
      <h1>Login Wall — Settings</h1>
      <p>
        A second page behind the fake login wall, reachable only from the dashboard link, not from
        any nav.
      </p>
    </main>
  );
}
