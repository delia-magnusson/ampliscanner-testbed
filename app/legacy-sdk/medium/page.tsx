import PlaceholderPage from "../../_components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title={"Legacy SDK — Medium"}
      description={"A page with a moderate amount of legacy SDK usage."}
      section={"legacy-sdk"}
      current={"/legacy-sdk/medium"}
    />
  );
}
