import PlaceholderPage from "../../_components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title={"PII — Form"}
      description={"A form page that will later track submitted field values, including PII such as email or phone number."}
    />
  );
}
